package client.core.system;

import client.core.model.Node;

public class ProxyFormView extends FormView implements client.core.model.ProxyFormView {
	public ProxyFormView(Key key, String label, boolean isDefault, Node scope) {
		super(key, label, isDefault, scope);
	}
}
